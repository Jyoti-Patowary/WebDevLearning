import { Box, Button } from '@mui/material';
import { useState } from 'react';

const Banner = () => {
    const [movie, setMovie] = useState();

    return (
        <Box>
            {/* <Button sx={{color: "red"}}>Play</Button>
            <Button sx={{color: "red"}}>Add to List</Button> */}


        </Box>
    );
};

export default Banner;